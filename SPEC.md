# Neta Stock（ネタストック）仕様書

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | Neta Stock（ネタストック） |
| 一言で言うと | 日常の「面白い出来事」「すべらない話」をサッとメモし、ストック・推敲できるお笑い特化型メモアプリ |
| プラットフォーム | iOS / Android（クロスプラットフォーム） |
| リリース方針 | テキスト中心のシンプル構成で開発コストを抑え、爆速でMVPをリリースする |

## 2. コンセプト

日常で起きた「面白い出来事」「すべらない話」「雑談のネタ」をサッとメモし、
ストック・推敲できるお笑い特化型メモアプリ。

## 3. ターゲットユーザー

- 普段の雑談や飲み会で面白いトークができるようになりたい人
- お笑い好き、ラジオ好き（ハガキ職人）、YouTuber・TikTokerなどのコンテンツクリエイター
- 芸人・お笑いサークル所属者

## 4. コア体験（提供価値）

1. **忘れない（爆速メモ）**
   思いついた瞬間、日常の「違和感」や「面白い出来事」を逃さず即座に記録できる。
2. **整理できる（構造化メモ）**
   「フリ（前提）」と「オチ（展開）」を意識して、話の構成をきれいに整理・ストックできる。
3. **取り出せる（サッと検索・準備）**
   飲み会や雑談の直前に、タグやキーワードで自分のネタを瞬時に検索・確認できる。

## 5. 開発方針

- 完全テキスト中心のシンプルで軽量なデータ構造により、開発コストを最小限に抑えつつ爆速でリリースする。
- 将来的にAI（Gemini API等）による「タイトル自動命名」や「オチ・フリの推敲・添削」を
  自然に組み込める拡張構造を持たせる。
- MVPでは画像・音声等のメディア添付、共有機能は扱わない（12章の将来拡張候補）。また、
  クラウド同期を前提としないローカル完結型の利用形態（アカウント登録なしで使い続けられる
  「オフライン専用モード」）も扱わない。11章の「オフライン時の閲覧・編集」は、あくまで
  クラウド同期を前提としたキャッシュ・再同期の仕組みであり、この除外事項とは異なる。

## 6. MVP機能仕様

### 6.1 ネタの基本操作（CRUD）
- ネタの新規作成、一覧表示、詳細表示、編集、削除ができる。
- **バリデーション**: `title`・`freeText`・`furi`・`ochi`・`point` の少なくとも1つに入力がなければ
  保存不可（すべて空のネタは作成できない）。`title`のみの入力でも保存可能とする
  （「タイトルだけ思いついた」メモも許容する）。
- **削除**: 誤操作防止のため削除前に確認ダイアログを表示する。取り消し（Undo）は設けない
  （MVPスコープ外、第11章の将来拡張候補）。
- **文字数上限**: `freeText`・`furi`・`ochi`・`point` は各2,000文字まで、`title`は100文字まで
  （Firestoreドキュメントサイズと入力UIの両面から極端な長文を防止する目安値。`title`は
  一覧で一行表示するため短めに制限する）。

### 6.2 フォーマット入力（構造化テンプレート）
- 「自由記述メモ」に加え、「フリ」「オチ」「ポイント」に分けて入力できるテンプレート機能。
- 自由記述のみでの入力も可能（構造化は任意）。入力モードは編集画面内でトグル切り替えできる。
- `title` を空にして保存した場合、一覧・検索では `freeText`→`furi`→`ochi`→`point` の順に
  最初に値が入っているフィールドの冒頭20文字程度を仮タイトルとして表示する
  （すべて空の場合はこの組み合わせでは発生しない。6.1のバリデーションにより
  `title`を含むいずれかのフィールドに入力があることが保証されるため）。

### 6.3 タグ付け・カテゴリ分類
- 「#バイト」「#家族」「#失敗談」「#あるある」などのタグでネタを整理できる。
- 1件のネタに複数タグを付与可能（上限: 10個/件）。
- タグは自由入力（自作タグ可）。既存タグはサジェスト表示し、入力の手間と表記ゆれを減らす。
- タグ文字列は前後の空白除去・小文字化などの正規化は行わない（ユーザー表記をそのまま尊重する）。

### 6.4 キーワード検索・フィルタリング
- 検索バーによる全件テキスト検索（`title`・`freeText`・`furi`・`ochi`・`point`を対象）。
- タグ指定による絞り込み表示（複数タグのAND/OR切り替えはMVPでは行わず、OR検索のみ）。
- 検索結果が0件の場合は「見つかりませんでした」の空状態を表示する。

### 6.5 お気に入り・ステータス管理
- ステータス: 「下書き」「完成」「披露済み」の3種。デフォルトは「下書き」。
  3状態間は順序に関係なく自由に往復切り替え可能（披露済みから下書きに戻す等も許容する）。
- よく使うネタの「お気に入り（ピン留め）」機能。一覧の上部に固定表示。
- お気に入り件数に上限は設けない。

## 7. 画面仕様

### 7.1 ログイン画面
- メールアドレス／パスワードによるサインイン・サインアップ切り替えタブ
- 入力バリデーション: メール形式チェック、パスワード6文字以上（Firebase Authenticationの
  デフォルト制約に準拠）
- エラー表示: 認証失敗時（該当アカウントなし／パスワード誤り／既存メール重複など）は
  Firebaseのエラーコードを日本語メッセージに変換して表示
- 「パスワードを忘れた方」導線（パスワードリセットメール送信）

### 7.2 ホーム（一覧）画面
- お気に入りセクション（横スクロールまたは上部固定）＋ネタ一覧（新着順デフォルト）
- 各行に: 仮/本タイトル、ステータスバッジ、タグ、更新日時
- 空状態: ネタが1件もない場合「最初のネタを記録しよう」という案内と作成ボタンを表示
- 右下または上部に「新規作成」ボタン（FAB等）
- データ取得はFirestoreのリアルタイムリスナー（`onSnapshot`）を用い、他デバイスでの変更も
  自動的に反映する。プルダウン操作は「再読み込み中」の心理的安心感を与えるUIとして用意するが、
  実データ取得は既にリアルタイムに行われているため、手動同期としての意味は持たない

### 7.3 ネタ詳細画面
- フリ／オチ／ポイント／自由記述／タグ／ステータスを表示
- 編集・削除・お気に入り切り替えへの導線
- ステータスはこの画面から直接変更可能（下書き・完成・披露済みの間を自由に往復可能、6.5参照）

### 7.4 ネタ作成・編集画面
- 自由記述モード／構造化モード（フリ・オチ・ポイント）をトグルで切り替え
- タグ入力欄（サジェスト付き）、ステータス選択
- 保存・キャンセルボタン。編集途中で画面を離れる場合は破棄確認ダイアログを表示
- 6.1のバリデーション（全項目空では保存不可）をこの画面で適用

### 7.5 検索画面
- キーワード検索バー＋タグフィルターチップ一覧
- 検索結果はホーム画面と同じ行コンポーネントで表示
- 0件時は空状態メッセージを表示（6.4参照）

### 7.6 設定画面
- アカウント情報（メールアドレス表示）
- ログアウト
- （将来拡張）アカウント削除、通知設定等

### 7.7 画面遷移図

```
[ログイン画面] --認証成功--> [ホーム(一覧)画面]

[ホーム(一覧)画面]
  ├─(新規作成ボタン)--> [ネタ作成画面] --保存/キャンセル--> [ホーム(一覧)画面]
  ├─(ネタ行タップ)----> [ネタ詳細画面]
  │                        ├─(編集)--> [ネタ編集画面] --保存/キャンセル--> [ネタ詳細画面]
  │                        └─(削除・確認OK)--> [ホーム(一覧)画面]
  ├─(検索バー/タブ)----> [検索画面] --(結果タップ)--> [ネタ詳細画面]
  └─(設定タブ)---------> [設定画面] --(ログアウト)--> [ログイン画面]
```

## 8. データモデル（Firestore）

```
users/{uid}
  - displayName: string
  - email: string
  - createdAt: Timestamp

users/{uid}/netas/{netaId}
  - title: string            // 最大100文字。空の場合は本文冒頭から自動仮タイトル表示（6.2参照、将来: AI自動命名）
  - freeText: string         // 自由記述メモ
  - furi: string             // フリ（前提）
  - ochi: string             // オチ（展開）
  - point: string            // ポイント・補足
  - tags: string[]           // 例: ["バイト", "あるある"]（上限10個）
  - status: "draft" | "completed" | "performed"
  - isFavorite: boolean
  - createdAt: Timestamp
  - updatedAt: Timestamp
```

- `freeText`・`furi`・`ochi`・`point`は各最大2,000文字、`title`は最大100文字（6.1参照）
- インデックス: `status`・`isFavorite`・`updatedAt`による絞り込み/並び替えに対応するため
  Firestore複合インデックスを用意する（一覧のデフォルトソートは`updatedAt`降順）
- 全文検索は簡易的にクライアント側でのフィルタリングで実現する（Firestoreの前方一致制約を
  考慮し、MVPでは高度な全文検索エンジンは導入しない。件数が増えた場合はAlgolia等の導入を
  将来検討）

## 9. Firestoreセキュリティルール（方針）

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;

      match /netas/{netaId} {
        allow read, write: if request.auth != null && request.auth.uid == uid;
      }
    }
  }
}
```

- 自分のUID配下のデータにのみ読み書きを許可し、他ユーザーのデータには一切アクセスできない。

## 10. 認証仕様

- Firebase Authentication（メール／パスワード方式）を採用
- サインアップ時に `users/{uid}` ドキュメントを作成し `displayName`・`email`・`createdAt` を保存
- パスワードリセットはFirebase標準のメール送信フローを利用
- ソーシャルログイン（Google等）はMVPスコープ外（将来拡張候補）

## 11. 非機能要件

- クラウド同期必須。データ取得・更新はFirestoreのリアルタイムリスナー（`onSnapshot`）を用いる
  （7.2参照）。オフライン時はFirestoreのローカルキャッシュにより閲覧・編集可能で、書き込みは
  端末内にキューイングされ、オンライン復帰時に自動的にサーバーへ反映される
- テキスト中心のためデータ量は軽量。画像・音声等のメディア添付はMVPスコープ外
- ユーザー間のデータは`users/{uid}`配下に隔離し、他ユーザーから参照不可（第9章）
- 通信エラー時は一覧・詳細画面にリトライ可能なエラー表示を行う
- 極端な低速回線を除き、一覧表示・保存操作は体感1秒以内の応答を目安とする

## 12. 将来拡張（MVP後）

- Gemini APIによるタイトル自動命名
- オチ・フリの推敲・添削アシスト
- ネタの共有機能（他ユーザーへの公開・エクスポート）
- 「披露済み」ネタの効果測定（ウケた/ウケなかった記録）
- 削除のUndo（一定時間内の取り消し）
- ソーシャルログイン（Google等）
- タグのAND検索、並び替えオプションの拡充

## 13. 用語集

| 用語 | 意味 |
|---|---|
| ネタ | ユーザーが記録する1件のエピソード・トークのこと |
| フリ | ネタの前提・導入部分 |
| オチ | ネタの結末・展開部分 |
| ポイント | フリ・オチ以外の補足メモ、話す際に意識したいポイント |
| ステータス | ネタの状態（下書き／完成／披露済み） |
| お気に入り | よく使うネタを一覧上部にピン留めする機能 |
